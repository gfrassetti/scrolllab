{
  "name": "hero-act",
  "private": true,
  "type": "module",
  "description": "VANTA hero act — Nuxt 3 + Vue 3 + three.js + GSAP + Lenis. One act, iterated to KPR level before the rest.",
  "scripts": {
    "dev": "nuxt dev --port 4100",
    "build": "nuxt build",
    "generate": "nuxt generate",
    "preview": "nuxt preview --port 4100",
    "postinstall": "nuxt prepare"
  },
  "dependencies": {
    "gsap": "^3.12.5",
    "lenis": "^1.1.14",
    "three": "^0.169.0",
    "vue": "^3.5.13"
  },
  "devDependencies": {
    "nuxt": "^3.14.159"
  }
}
